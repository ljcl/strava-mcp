{
  "extends": ["//"],
  "tags": ["app"],
  "tasks": {
    "test": {
      "dependsOn": ["topo", "^build"]
    },
    "test:coverage": {
      "dependsOn": ["topo", "^build"],
      "outputs": ["coverage/**"]
    }
  }
}
