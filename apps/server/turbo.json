{
  "extends": ["//"],
  "tags": ["app"],
  "tasks": {
    "test": {
      "dependsOn": ["topo", "^build"],
      "env": ["UPDATE_TOOL_SURFACE_LOCK"]
    },
    "test:coverage": {
      "dependsOn": ["topo", "^build"],
      "outputs": ["coverage/**"],
      "env": ["UPDATE_TOOL_SURFACE_LOCK"]
    }
  }
}
